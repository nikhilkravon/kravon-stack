# Set these as environment variables per deployment (one deployment per tenant in production)
# For local dev: leave RESTAURANT_SLUG empty and use ?slug=your-slug in the URL instead
BACKEND_URL=https://api.kravon.in
FRONTEND_URL=https://your-restaurant-slug.kravon.in
RESTAURANT_SLUG=
PORT=8000
